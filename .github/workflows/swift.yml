# This workflow will build the LearnKeys Swift project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-swift

name: Swift

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Swift
      uses: swift-actions/setup-swift@v1
      with:
        swift-version: "5.9"
    
    - name: Make build script executable
      run: chmod +x build.sh
      
    - name: Build LearnKeys
      run: ./build.sh
    
    - name: Verify build output
      run: |
        if [ -f "build/LearnKeys" ]; then
          echo "✅ Build artifact created successfully"
          ls -la build/LearnKeys
        else
          echo "❌ Build artifact not found"
          exit 1
        fi
    
    - name: Run basic build tests
      run: |
        chmod +x Tests/test_build.sh
        Tests/test_build.sh
    
    - name: Run comprehensive logic tests
      run: |
        chmod +x Tests/test_logic.sh
        Tests/test_logic.sh
    
    - name: Upload build artifact
      uses: actions/upload-artifact@v3
      with:
        name: LearnKeys-executable
        path: build/LearnKeys
